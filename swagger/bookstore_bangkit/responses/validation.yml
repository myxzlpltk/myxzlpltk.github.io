description: Form validation error
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: boolean
          example: false
        message:
          type: string
          example: Form validation error
        errors:
          type: object
          properties:
            field:
              type: array
              items:
                type: string
                example: This field is required